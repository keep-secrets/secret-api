apiVersion: apps/v1
kind: Deployment
metadata:
  name: $APP_NAME-api
  labels:
    app: $APP_NAME-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: $APP_NAME-api
  template:
    metadata:
      labels:
        app: $APP_NAME-api
    spec:
      containers:
        - name: $APP_NAME-api
          image: $REGISTRY_URI/$APP_NAME:latest
          env:
            - name: SECRET
              value: "$SECRET"
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: $APP_NAME-lb
spec:
  selector:
    app: $APP_NAME-api
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
